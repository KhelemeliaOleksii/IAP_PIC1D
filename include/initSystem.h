#ifndef INITSYSTEM_H
    #define INITSYSTEM_H
        //DO:   - check if we need to continue calculation
        //      - if (needContinue == 1) 
        int InitSystem (int needContinue);
#endif //INITSYSTEM_H